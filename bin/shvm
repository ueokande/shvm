#!/bin/sh
set -e

cat <<EOS
shvm is invoked as command.  The installation is failed.
Add the following line to $HOME/.bash_profile, and restart your shell.

  source $SHVM_HOME/bin/init-shvm

EOS

exit 1
